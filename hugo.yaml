baseURL: https://salamivf.com/
title: Dr Salam Jibrel Medical Center

# Google Analytics
services:
  googleAnalytics:
    id: G-1XGJRQ376C

# cSpell:ignore goldmark github hugo readingtime docsy subdir lastmod pygments linenos catmullrom slugorcontentbasename

# Language settings
contentDir: content/en
defaultContentLanguage: en
defaultContentLanguageInSubdir: true
# Useful when translating.
enableMissingTranslationPlaceholders: true

enableRobotsTXT: true

# Will give values to .Lastmod etc.
enableGitInfo: true

# Comment out to disable taxonomies in Docsy
# disableKinds: [taxonomy]

# You can add your own taxonomies
taxonomies:
  tag: tags
  category: categories

# Highlighting config
pygmentsCodeFences: true
pygmentsUseClasses: false
# Use the new Chroma Go highlighter in Hugo.
pygmentsUseClassic: false
# pygmentsOptions: "linenos=table"
# See https://help.farbox.com/pygments.html
pygmentsStyle: tango

mediaTypes:
  text/redirects:
    delimiter: ''

outputFormats:
  redirects:
    baseName: _redirects
    isPlainText: true
    mediaType: text/redirects
    root: true

# Configure how URLs look like per section.
permalinks:
  blog: /:section/:year/:month/:day/:slugorcontentbasename

# Image processing configuration.
imaging:
  resampleFilter: CatmullRom
  quality: 75
  anchor: smart

# Language configuration
languages:
  en:
    languageName: English
    title: Dr Salam Jibrel Medical Center
    weight: 1
    params:
      description: Leading Fertility & IVF Center in Bahrain
  ar:
    languageDirection: rtl
    languageName: 'العربية'
    contentDir: content/ar
    title: 'مركز د. سلام جبريل الطبي'
    weight: 2
    params:
      description: 'مركز رائد للخصوبة وأطفال الأنابيب في البحرين'
      time_format_default: 02.01.2006
      time_format_blog: 02.01.2006

markup:
  goldmark:
    parser:
      attribute:
        block: true
    renderer:
      unsafe: true
  highlight:
    # See a complete list of available styles at https://xyproto.github.io/splash/docs/all.html
    style: tango
    noClasses: false # Required for dark-mode support of code blocks
    # Uncomment if you want your chosen highlight style used for code blocks without a specified language
    # guessSyntax: true

# Everything below this are Site Params

# Comment out if you don't want the "print entire section" link enabled.
outputs:
  home: [HTML, print, RSS, redirects]
  section: [HTML, print, RSS]

params:
  # Google Tag Manager ID (get from tagmanager.google.com)
  gtm_id: GTM-NN5F93P

  taxonomy:
    # set taxonomyCloud = [] to hide taxonomy clouds
    taxonomyCloud: [tags, categories]

    # If used, must have same length as taxonomyCloud
    taxonomyCloudTitle: [Tag Cloud, Categories]

    # set taxonomyPageHeader = [] to hide taxonomies on the page headers
    taxonomyPageHeader: [tags, categories]

  privacy_policy: /privacy/

  # First one is picked as the Twitter card image if not set on page.
  # images: [images/project-illustration.png]

  # Enable Lunr.js offline search
  offlineSearch: true

  # Enable syntax highlighting and copy buttons on code blocks with Prism
  prism_syntax_highlighting: false

  copyright:
    authors: Dr Salam Jibrel Medical Center
    from_year: 2017
    to_year: 2026

  # User interface configuration
  ui:
    # Set to true to disable breadcrumb navigation.
    breadcrumb_disable: false
    # Set to false if you don't want to display a logo (/assets/icons/logo.svg) in the top navbar
    navbar_logo: true
    # Set to true if you don't want the top navbar to be translucent when over a `block/cover`, like on the homepage.
    navbar_translucent_over_cover_disable: false

    sidebar_menu_foldable: true
    # Enable to show the side bar menu in its compact state.
    sidebar_menu_compact: false
    # Set to true to hide the sidebar search box (the top nav search box will still be displayed if search is enabled)
    sidebar_search_disable: false
    # Enable light/dark mode toggle menu in the navbar
    showLightDarkModeMenu: true

    # Adds a H2 section titled "Feedback" to the bottom of each doc. The responses are sent to Google Analytics as events.
    # This feature depends on [services.googleAnalytics] and will be disabled if "services.googleAnalytics.id" is not set.
    # If you want this feature, but occasionally need to remove the "Feedback" section from a single page,
    # add "hide_feedback: true" to the page's front matter.
    feedback:
      enable: true
      yes: Glad to hear it! Please <a href="https://github.com/laravel-vibes/salam-static-site/issues/new">tell us how we can improve</a>.
      no: Sorry to hear that. Please <a href="https://github.com/laravel-vibes/salam-static-site/issues/new">tell us how we can improve</a>.

    # Adds a reading time to the top of each doc.
    # If you want this feature, but occasionally need to remove the Reading time from a single page,
    # add "hide_readingtime: true" to the page's front matter
    readingtime:
      enable: false
  # Docs helpers: use with _param
  btn-lg: class="btn btn-lg btn-{1}" role="button"
  FA: <i class="fa-{1} fa-{2} text-{3} px-1"></i>

  links:
    user:
      - name: Phone
        url: 'tel:+97317000000'
        icon: fa fa-phone
        desc: Call us for appointments
      - name: Email
        url: 'mailto:info@salamjibrel.com'
        icon: fa fa-envelope
        desc: Email us for inquiries
      - name: Location
        url: 'https://maps.google.com'
        icon: fa fa-map-marker-alt
        desc: Find us in Bahrain
    developer:
      - name: Instagram
        url: 'https://instagram.com'
        icon: fab fa-instagram
        desc: Follow us on Instagram
      - name: WhatsApp
        url: 'https://wa.me/97317000000'
        icon: fab fa-whatsapp
        desc: Message us on WhatsApp

security:
  funcs: # cspell:disable-line
    getenv: # cspell:disable-line
      # Netlify build env var
      - ^(BRANCH|BUILD_ID|COMMIT_REF|CONTEXT|DEPLOY_ID|NETLIFY|PULL_REQUEST|REVIEW_ID)$

module:
  # Uncomment the next line to build and serve using local docsy clone declared in the named Hugo workspace:
  # workspace: docsy.work
  hugoVersion:
    extended: true
    min: 0.155.0
  imports:
    - path: github.com/google/docsy
      disable: false
